JSR303 是一套JavaBean参数校验的标准
@Null 必须为null
@NotNull 
@AssertTrue 必须为true
@AssertFalse
@Min(value)
@Max(value)
@DecimalMin(value)
@DecimalMax(value)
@Size(max,min)
@Digits(Integer,fraction) 必须为一个数字在可接受的范围内
@Past 必须是一个过去的日期
@Future 
@Pattern(value) 符合正则表达式
Hibernate validator 在JSR303的基础上对校验注解进行了扩展，扩展注解如下：
@Email 
@Length 字符串大小长度
@NoEmpty
@Range
Spring validtor 同样扩展了jsr303,并实现了方法参数和返回值的校验
